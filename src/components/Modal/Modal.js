import React from 'react';
import withBackgroundBlur from '../HOC/withBackgroundBlur';
import Overlay from './Overlay';

function Modal({ children, handleClose }) {
  return
}

export default withBackgroundBlur(Modal);
